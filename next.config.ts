import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['i.imgur.com', 'placeimg.com', 'img.com', 'image.com', 'iagedive.com', 'krishnaImage.com', 'krishnaimage.com'],
  }
};

export default nextConfig;
